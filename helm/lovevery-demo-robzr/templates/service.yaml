apiVersion: v1
kind: Service
metadata:
  name: {{ include "lovevery-demo-robzr.fullname" . }}
  labels:
    {{- include "lovevery-demo-robzr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "lovevery-demo-robzr.selectorLabels" . | nindent 4 }}
